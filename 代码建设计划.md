# 光热发电定日镜场优化设计 — 代码建设计划

> **目标读者**：将执行编码的 AI Agent 或程序员
> **任务来源**：数学建模题 — 《定日镜场的优化设计》(Solar Tower Heliostat Field Optimization)
> **文档生成**：2026-07-24

---

## 〇、数据筛查与干扰识别

### 0.1 输入图片清单与内容识别

| # | 截图内容 | 关键公式 / 参数 | 用途 |
|---|---------|----------------|------|
| 1 | 太阳位置模型 | δ, ω, α_s, γ_s 公式 | 任意时刻太阳位置计算 |
| 2 | 阴影遮挡与截断效率 | η_sb, η_trunc, η_flux 定义 | 单镜几何损耗 |
| 3 | 研究背景与业务逻辑 | 流程图 | 无公式（背景） |
| 4 | DNI 直接辐射模型 | DNI = G_0[a + b·exp(-c/sin α_s)] | 法向辐照度 |
| 5 | 光学效率分解 | η = η_sb·η_cos·η_at·η_trunc·η_ref | 综合光学效率 |
| 6 | 课程封面页 | 无 | 干扰，应忽略 |
| 7 | 问题重述与三问路线图 | 塔位/尺寸/高/数量 优化 | **核心问题定义** |
| 8 | 镜场坐标系与几何结构 | 半径 350m、禁区 100m、约束 | 几何边界条件 |
| 9 | 镜面法向与反射几何 | n̂ = (ŝ+r̂)/|ŝ+r̂| | 反射定律 |
| 10 | 模型假设与变量符号说明 | ρ=0.92、北纬 39.4°、海拔 3000m | 总体参数与约束 |

### 0.2 干扰数据筛除清单 ⚠️

| 类型 | 来源 | 处理方式 |
|------|------|----------|
| 讲师口头标注 | "算近日怎么转OK"、"这一页是阴影遮挡..." | **删除** |
| 课程水印 | "数学建模速成课"、"Bilibili" Logo | **删除** |
| 课程封面页 | 图片 6（标题页/讲师名） | **不引用** |
| 商业模式表述 | "碳达峰碳中和"、"双碳目标" | 仅用作引言，不进入数学模型 |
| 截图中右下角进度圈 | "00:16" 等倒计时 | **删除** |

### 0.3 关键参数一致性校核 ⚠️

| 参数 | 出处 | 值 | 一致性 |
|------|------|----|--------|
| 纬度 φ | 图片 1、10 | 39.4°N | ✅ 一致 |
| 海拔 H | 图片 4、10 | 3.0 km | ✅ 一致 |
| 太阳常数 G_0 | 图片 4 | 1.366 kW/m² | ✅ 标准值 |
| 反射率 ρ | 图片 10 | 0.92 | ✅ 题目给定 |
| 额定功率 | 图片 7 | 60 MW | ✅ 三问统一 |
| 基准镜尺寸 | 图片 7、10 | 6m × 6m 正方形 | ✅ 问题一基准 |
| 镜场半径 | 图片 8 | 350 m | ✅ |
| 禁区半径 | 图片 8 | 100 m | ✅ |
| 塔高 | 图片 8 | 80 m | ✅ |
| 吸热器尺寸 | 图片 8 | 高 8 m，直径 7 m（圆柱） | ✅ |
| 安装高度 | 图片 10 | h_i（下边缘离地） | ⚠️ 注意：问题一中"安装高度4m"应理解为 h_i=4m |
| 时间采样 | 图片 10 | 每月21日 9:00/10:30/12:00/13:30/15:00 → 共 60 个时刻 | ✅ |
| 积日 D | 图片 1、10 | "从春分起算"，即 D = N - 81（N为年内日期） | ⚠️ **关键歧义**：图片 1 同时写了 "积日 D：从春分（约3月21日）起算"，需确认 |

> **积日 D 的两种理解**：
> - **理解 A**（推荐）：D = N - 80，N为年内日期序号（1-365），春分日 N=80 时 D=0
> - **理解 B**（备选）：D 直接以春分为第 1 天（即 D ∈ [1, 365]，春分 D=1）
> **建议在 `solar_position.py` 中以 `D=A-80` 默认实现，并提供可切换参数 `D_convention='from_spring_equinox'`**

---

## 一、问题建模总览

### 1.1 三问题对比

| 问题 | 类型 | 关键变量 | 目标 |
|------|------|---------|------|
| **问题一** | 正向模拟（基准） | 塔在中心、6×6m、h=4m、圆环/栅格 | 计算 η̄ 年平均、P̄ 年平均 |
| **问题二** | 均匀优化（统一参数） | (x_t,y_t)、W=H、h_inst、N、布局 | max P̄_mirror = P_total / (N·W²) 约束 P̄=60MW |
| **问题三** | 异构优化（差异化） | 每镜独立 (x_i,y_i,W_i,H_i,h_i) | 在 P̄≥60MW 约束下，最大化 min(P̄_mirror,某种分位) 或期望值 |

### 1.2 核心目标函数

```
P_field = Σ_i Σ_t [ A_i · DNI(t) · η_i(t) ] · Δt
η_i(t) = ρ · η_cos_i(t) · η_sb_i(t) · η_trunc_i(t) · η_at(t)
P̄_field = 年加权平均 = (1 / T_year) · Σ_t P(t) · Δt
η̄_field = P̄_field / (A_total · DNI_avg)
```

### 1.3 关键决策点

| 决策点 | 选项 | 推荐 | 理由 |
|--------|------|------|------|
| 编程语言 | Python / MATLAB / Julia | **Python** | NumPy/SciPy 向量化、生态成熟 |
| 优化算法 | SLSQP / DE / NSGA-II / PSO | **DE + 局部精修** | 解析梯度缺失、离散+连续混合变量 |
| 阴影遮挡算法 | 解析 / 蒙特卡洛 / 栅格采样 | **栅格预计算 + 矢量化** | 速度与精度平衡 |
| 截断判断 | 圆锥相交 / 角度边界 | **圆锥 + 接收器圆盘求交** | 几何精确 |
| 时间采样 | 60 时刻加权平均 | **按日照时长加权** | 避免早晚极端时刻主导 |
| 布局方式 | 径向 / 矩形栅格 / 螺旋 | **径向同心 + 角度交错** | 经典工程方案 |
| 异构分组 | 离散优化 / 分区连续 | **分环分区 + 内层连续优化** | 减少搜索空间 |

---

## 二、技术栈与依赖

```toml
# requirements.txt 建议
numpy >= 1.26
scipy >= 1.11
matplotlib >= 3.8
plotly >= 5.18           # 交互可视化
pandas >= 2.1
tqdm >= 4.66
numba >= 0.58            # 热函数 JIT 加速
joblib >= 1.3            # 并行

# 优化
pymoo >= 0.6             # 多目标/差分进化
```

**Numba 加速**：阴影遮挡和截断判断是热点函数，建议 JIT。

---

## 三、模块化架构总览

```
csp_heliostat/
├── config/                 # 参数配置（可切换）
│   ├── constants.py
│   └── sampling.py
├── core/                   # 基础物理模型（纯函数）
│   ├── solar_position.py
│   ├── dni.py
│   ├── geometry.py
│   └── atmosphere.py
├── field/                  # 镜场建模
│   ├── layout.py
│   ├── mirror.py
│   └── constraints.py
├── efficiency/             # 五项效率
│   ├── cosine.py
│   ├── shadow_blocking.py
│   ├── truncation.py
│   ├── atmospheric.py
│   └── composition.py
├── simulation/             # 仿真器
│   ├── snapshot.py
│   └── annual.py
├── optimization/           # 优化求解
│   ├── uniform.py
│   ├── zoned.py
│   └── heterogeneous.py
├── visualization/          # 可视化
│   ├── layout_plot.py
│   └── efficiency_plot.py
├── tests/                  # 单元测试
└── main_problem1.py / main_problem2.py / main_problem3.py
```

---

## 四、各模块详细接口与实现要点

### 4.1 `config/constants.py` — 全局常量

```python
"""全局常量与基础参数。所有模块共享。"""
from dataclasses import dataclass

# 场址与大气
LATITUDE_DEG = 39.4          # 北纬
ALTITUDE_KM = 3.0
SOLAR_CONSTANT_W_M2 = 1366.0

# 几何边界
FIELD_RADIUS_M = 350.0
EXCLUSION_RADIUS_M = 100.0
TOWER_HEIGHT_M = 80.0
RECEIVER_HEIGHT_M = 8.0
RECEIVER_DIAMETER_M = 7.0

# 镜面设计范围
MIRROR_W_RANGE = (2.0, 8.0)
MIRROR_H_RANGE = (2.0, 8.0)
INSTALL_HEIGHT_RANGE = (2.0, 6.0)
ADJACENT_CLEARANCE_M = 5.0    # d > W + 5

# 反射率
REFLECTIVITY = 0.92

# 时间采样
SAMPLE_TIMES_HOUR = [9, 10.5, 12, 13.5, 15]
SAMPLE_DAY_OF_MONTH = 21      # 每月21日

# 额定功率
RATED_POWER_MW = 60.0

# 仿真精度
RAY_SAMPLES_PER_M2 = 4        # 阴影遮挡光线密度
NUM_RAYS_SHADOW = 25          # 每镜光线数
```

---

### 4.2 `config/sampling.py` — 时间采样表

```python
"""生成 60 个采样时刻：(month, day, hour) + 权重。
对每时刻计算年积日 D 与太阳角。"""
from typing import List, Tuple

def build_sampling_schedule() -> List[Tuple[int, float, float]]:
    """返回 [(month, day_of_month, hour_local), ...] 共 12×5=60 时刻。
    默认每月 21 日，5 个时刻。"""

def day_of_year(month: int, day: int) -> int:
    """Gregorian → day-of-year (1-365)。"""

def day_from_spring_equinox(doy: int) -> int:
    """积日 D 从春分起算：D = doy - 80（D=0 当 3 月 21 日）。"""

def solar_weights(samples) -> np.ndarray:
    """每时刻的年权重 ∝ 1/(sin α_s)，模拟实际日照贡献。
    注：这是物理上更严谨的加权，避免日出日落低角主导平均。
    若采用等权平均，文档需注明其低估了高角时段贡献。"""
```

---

### 4.3 `core/solar_position.py` — 太阳位置模型

**输入**：纬度 φ、积日 D、当地时间 t（小时）
**输出**：太阳高度角 α_s、太阳方位角 γ_s（南=0，东为正）
**依赖**：纯数学，无外部依赖

```python
import numpy as np

def declination(D: np.ndarray) -> np.ndarray:
    """δ = 23.45° · sin(2π(284+D)/365)
    D: 积日（从春分起算，可为负）
    返回：赤纬角（度）"""

def hour_angle(t_hour: np.ndarray) -> np.ndarray:
    """ω = 15° · (t - 12)
    返回：时角（度）"""

def solar_altitude(phi_deg, delta_deg, omega_deg) -> np.ndarray:
    """sin α_s = sin φ sin δ + cos φ cos δ cos ω
    返回：高度角（度），clamp 到 [-1, 1]"""

def solar_azimuth(phi_deg, delta_deg, omega_deg) -> np.ndarray:
    """γ_s = atan2(sin ω, cos ω sin φ - tan δ cos φ)
    返回：方位角（度），从南顺时针为正（南=0，东=+90°）。"""

@dataclass
class SunState:
    altitude_deg: np.ndarray
    azimuth_deg: np.ndarray
    sin_altitude: np.ndarray  # 预计算避免重复

def sun_state_batch(phi_deg, D, t) -> SunState:
    """批处理：给定 (N,D) 数对，返回对应 SunState。
    凡 α_s ≤ 0 标记为'夜晚'，下游需跳过。"""
```

**单元测试**：
- 春分正午 (D=0, t=12, φ=39.4°)：α_s ≈ 50.6°
- 夏至正午 (D≈93, t=12, φ=39.4°)：α_s ≈ 73.55°
- 冬至正午：α_s ≈ 26.95°

---

### 4.4 `core/dni.py` — 直接法向辐照度模型

**输入**：太阳高度角 α_s、海拔 H、太阳常数 G_0
**输出**：DNI (W/m²)

```python
import numpy as np

def dni_coefficients(H_km: float) -> Tuple[float, float, float]:
    """返回 (a, b, c) 三参数，海拔三次多项式。"""

def direct_normal_irradiance(alpha_s_deg, H_km=SOLAR_CONSTANT_H_KM,
                              G0=1366.0) -> np.ndarray:
    """DNI = G_0 · [a + b · exp(-c / sin α_s)]
    边界处理：
      - sin α_s → 0 时应用极限切线（约 0.6·G_0·cos(π/2-π/8)，即 86.25° 后视为 0）
      - 负值裁剪至 0。
      - 输入 α_s 单位度，必须为正（夜晚设为 0）。"""
```

**关键实现要点**：
```python
# 极限处理：当 sin α_s 接近 0 时，a + b·exp(-c/sin α_s) 会爆炸
# 实现：
mask = sin_alpha > 1e-3
out = np.where(mask,
              a + b * np.exp(-c / sin_alpha),
              np.maximum(a + b * np.exp(-c / 1e-3) * (sin_alpha/1e-3), 0))
out = np.clip(out, 0, 1.5) * G0  # 上限防止数值发散
```

---

### 4.5 `core/geometry.py` — 反射几何

**核心公式**：n̂ = (ŝ + r̂) / |ŝ + r̂|

```python
import numpy as np

def sun_unit_vector(altitude_deg, azimuth_deg) -> np.ndarray:
    """从太阳角转 3D 单位向量。
    约定：
      x 轴 → 东
      y 轴 → 北
      z 轴 → 天顶
    ŝ 指向太阳：s_x = cos α sin γ, s_y = cos α cos γ, s_z = sin α
    注：图中方位是从南顺时针为正；本公式采用东=0、北=π/2 的常用约定，二者需映射。
    为避免歧义：内部统一使用 [东, 北, 天] 系，外部输入 γ_s=0 表示正南。"""

def receiver_vector_from_mirror(mirror_xy, receiver_xy, mirror_height):
    """从 M(x_i, y_i, h_i) → R(x_t, y_t, H_R_top)。
    返回单位向量 r̂。"""

def mirror_normal(s_hat, r_hat) -> np.ndarray:
    """反射定律：n̂ = (ŝ + r̂) / |ŝ + r̂|
    输入：Nx3 单位向量
    返回：Nx3 单位向量"""

def cosine_efficiency(n_hat, s_hat) -> np.ndarray:
    """η_cos = max(0, n̂ · ŝ)，即入射角余弦。
    当 n̂ · ŝ < 0 时（即太阳在镜面背后），返回 0。"""
```

**关键约定**：
- **坐标**：东(x)、北(y)、天(z)，右手系
- **方位角方向**：从正南顺时针为正（题目原文），输入 API 需文档化
- **法向方向**：镜像法向只取"朝上"分量（即 z 分量 ≥ 0），否则无意义

---

### 4.6 `core/atmosphere.py` — 大气透过率

**输入**：斜路径长度、海拔、大气参数
**输出**：η_at

```python
def atmospheric_transmittance(distance_m, H_km=3.0) -> np.ndarray:
    """采用 Beer-Lambert 简化模型：
       η_at = exp(-τ · ρ_air · path_length)
    τ：消光系数（约 0.1 ~ 0.2，高海拔低）
    ρ_air：相对密度 = exp(-H/8.5)
    
    实用模型（推荐，工程常用）：
       η_at = 0.99321 - 0.0001176·d + 1.97e-8·d²  
    其中 d 为镜-塔距离 (m)。来源：DLR / Sandia 经验式。
    
    简化：d = sqrt((x_i-x_t)² + (y_i-y_t)² + (h_inst - H_t)²)
    """
```

**重要**：题目原文未给具体大气公式，须自行选择。**建议采用 Sandia 经验式**，并在文档中标注来源。

---

### 4.7 `field/layout.py` — 镜场布局生成器

**核心职责**：生成 N 块镜面的 (x_i, y_i, h_i)。

```python
from typing import List, Tuple
import numpy as np

@dataclass
class Mirror:
    x: float      # 东向坐标
    y: float      # 北向坐标
    width: float  # W
    height: float # H
    install_height: float  # h_i（下边缘离地）
    # 派生
    center_z: float         # z = h_i + H/2

def radial_layout(n_rings: int, n_per_ring: int,
                  R_inner=100, R_outer=350,
                  azimuth_offset=0.0) -> List[Mirror]:
    """径向同心布局（推荐基线）：
       - 环半径 r_k = R_inner + (k+0.5)·Δr，k=0..n_rings-1
       - 每环角度数 n_k，随半径增加而增多（避免内密外疏）
       - 角度交错：奇数环旋转半格
    
    注：返回 width=height=6, install_height=4 作为问题一基准。
    """

def grid_layout(n_rows, n_cols, dx, dy, x0, y0) -> List[Mirror]:
    """矩形栅格布局（用于均匀优化初始解）。"""

def spiral_layout(n_mirrors, R_inner, R_outer) -> List[Mirror]:
    """螺旋布局（金门型，理论上风阻小）：可选。"""

def exclude_zone(mirrors, tower_xy=(0,0), exclusion_r=100) -> List[Mirror]:
    """剔除禁区（厂区建筑物）内的镜。"""

def spacing_check(mirrors, min_clearance=5.0) -> List[Tuple[int, int]]:
    """检查相邻距约束 d_ij > W_i + W_j + 5m。
    返回违规对下标列表。"""
```

**关键算法 — 径向布局密度公式**：

```python
# 每环镜面数量按周长比例分配：
# 2π·r_k = n_k · (W + clearance)
n_k = int(np.ceil(2 * np.pi * r_k / (W + min_clearance)))
```

---

### 4.8 `efficiency/cosine.py` — 余弦效率

```python
def cosine_for_field(mirrors, sun: SunState, tower_xy) -> np.ndarray:
    """对每面镜在指定太阳位置下计算 η_cos_i。
    即 n̂_i · ŝ，clamp 到 [0, 1]。
    """
```

---

### 4.9 `efficiency/shadow_blocking.py` — 阴影遮挡

**方法**：栅格预计算 + 矢量化判定

```python
def build_shadow_map(mirrors, sun: SunState, grid_res=0.5) -> np.ndarray:
    """在场地平面上布栅格，对每面镜投射阴影，看哪些被遮挡。
    返回 N×M bool 矩阵：被遮挡栅格数。"""

def shadow_blocking_efficiency(mirrors, sun: SunState) -> np.ndarray:
    """η_sb_i = (总栅格 - 被遮挡栅格) / 总栅格
    简化（推荐工程方案）：
       - 镜面均匀划分为 N×N 子网格（默认 8×8）
       - 每子格中心投射沿太阳反方向投影到场平面
       - 检查投影点是否落在相邻镜面的几何投影中
    
    性能：用 Numba @njit 加速内层循环。
    """
```

**关键实现**：避免对每对镜都做完整 N² 循环，建议：
1. **空间索引**：用 KD-tree 找到附近镜（cKDTree from scipy.spatial）
2. **预计算**：对常用太阳位置预存阴影图，按内插加速

---

### 4.10 `efficiency/truncation.py` — 截断效率

```python
def truncation_efficiency(mirrors, sun: SunState, tower_xy,
                          receiver_height=8.0,
                          receiver_radius=3.5) -> np.ndarray:
    """η_trunc_i = A_inside / A_mirror
    算法：
       1. 镜面在自身坐标系中细分为子网格
       2. 每子格中心发射反射光（沿 r̂）
       3. 求与吸热器圆柱面的相交
       4. 落入 [H_R_bottom, H_R_top] 范围且半径 < R_R 视为接收
    
    工程简化：
       - 由于 η_cos 已隐含入射损失，截断主要反映镜尺寸 vs 接收器尺寸失配
       - 可用解析公式：η_trunc = min(1, (H_R·W_R)/(d²·tan²(θ_i))) 估算
       - 建议两种方法对比验证
    """
```

---

### 4.11 `efficiency/composition.py` — 总效率装配

```python
def total_efficiency(mirrors, sun: SunState, tower_xy) -> np.ndarray:
    """η_i = ρ · η_cos · η_sb · η_trunc · η_at
    返回每镜标量效率。"""

def field_efficiency(eta_i, A_i) -> float:
    """η_field = Σ(η_i · A_i) / Σ A_i（按面积加权）"""
```

---

### 4.12 `simulation/snapshot.py` — 单时刻仿真

```python
def simulate_one(mirrors, sun: SunState, tower_xy,
                 H_km=3.0) -> dict:
    """Returns: {
        'eta_cos': ndarray,
        'eta_sb': ndarray,
        'eta_trunc': ndarray,
        'eta_at': ndarray,
        'eta_total': ndarray,
        'power_w': float,         # Σ DNI·A_i·η_i
        'eta_field_avg': float,   # 面积加权平均
    }"""
```

---

### 4.13 `simulation/annual.py` — 年平均仿真

```python
def annual_simulation(mirrors, tower_xy,
                      phi_deg=LATITUDE_DEG,
                      H_km=ALTITUDE_KM,
                      schedule=None) -> dict:
    """对 60 个时刻逐点仿真，按 sin α_s 权重积分得到年平均。
    
    Returns: {
        'eta_field_avg': float,   # 年平均光学效率
        'P_field_avg_MW': float,  # 年平均输出热功率
        'per_sample': List[dict], # 每时刻详情
    }
    """
```

---

### 4.14 `optimization/uniform.py` — 问题二优化器

**决策变量**（连续）：
- 塔位置 (x_t, y_t) ∈ [−100, 100]²
- 镜面边长 W = H ∈ [2, 8]
- 安装高度 h_inst ∈ [2, 6]
- 环数 n_rings ∈ {5, 6, ..., 30}（离散）
- 每环镜数 n_per_ring ∈ ℤ⁺（由周长公式导出）

**目标**：max P_mirror = P_total / (N · W²)
**约束**：
- P_total = 60 MW（等式）
- d_ij > W + 5（不等式）
- (x_t, y_t) 在场中心合理范围内

```python
def objective_uniform(params: np.ndarray) -> float:
    """params = [x_t, y_t, W, h_inst, n_rings]
    返回 -P_mirror（最小化目标）"""

def constraint_power(params) -> float:
    """P_total - 60e6 ≥ 0（罚函数形式）"""

def objective_with_penalty(params) -> float:
    """目标 + 大数法处理违规约束"""

# 推荐算法
from scipy.optimize import differential_evolution
result = differential_evolution(
    objective_with_penalty,
    bounds=[(-100,100), (-100,100), (2,8), (2,6), (5, 30)],
    maxiter=200,
    popsize=30,
    tol=1e-6,
)
```

**关键决策 — n_rings 与每环镜数的关系**：

```python
# 由周长公式推：每环镜数 n_k = ceil(2π·r_k / (W + 5))
# 总镜数：Σ n_k = f(n_rings, W)
# 布局时还考虑"奇偶环交错"
```

---

### 4.15 `optimization/heterogeneous.py` — 问题三优化器

**决策变量**（高维，每镜独立）：
- (x_i, y_i, W_i, H_i, h_i) for i ∈ [0, N-1]
- 总维度 = 5N

**分阶段策略**（推荐）：

```
Phase 1: 用问题二的均匀最优解作为初值（warm start）
Phase 2: 按"径向环"分组，组内共享参数（zoned）
   - 决策变量 = (r_ring, W_ring, h_ring) × n_rings + (tower_xy)
Phase 3: 微调 — 在 Phase 2 解附近做邻域搜索（局部优化）
```

**算法选型**：
- **Phase 2**：差分进化（DE） — 适合中等维（≤ 30）
- **Phase 3**：CMA-ES 或 L-BFGS-B 微调

```python
def zoned_optimization(groups: List[int]) -> dict:
    """groups[i] 给出镜 i 所属的环索引。
    返回最佳参数与对应镜场配置。"""

def heterogeneous_refine(initial_solution, perturbation_radius=0.3):
    """以初始解为中心做局部搜索。
    算法：模拟退火 / 模式搜索 (Hooke-Jeeves)。
    """
```

---

### 4.16 `visualization/layout_plot.py`

```python
def plot_field(mirrors, tower_xy=(0,0), save_path=None) -> None:
    """matplotlib 平面图：
       - 蓝色方块表示镜面（朝向画法向向量）
       - 红色圆环 = 塔
       - 灰色圆 = 禁区
       - 白色虚线 = 镜场半径"""

def plot_efficiency_heatmap(mirrors, eta_i) -> None:
    """用 plotly 散点图，色编码 η_i"""

def plot_annual_efficiency_curve(samples) -> None:
    """60 时刻的 η 与 t 关系图"""
```

---

### 4.17 主入口脚本

```python
# main_problem1.py
from csp_heliostat.simulation.annual import annual_simulation
from csp_heliostat.field.layout import radial_layout, Mirror

mirrors = radial_layout(n_rings=18, W=6, install_h=4)
result = annual_simulation(mirrors, tower_xy=(0, 0))
print(f"年平均光学效率: {result['eta_field_avg']:.4f}")
print(f"年平均输出热功率: {result['P_field_avg_MW']:.2f} MW")
```

```python
# main_problem2.py
from csp_heliostat.optimization.uniform import solve_problem2
result = solve_problem2(target_power_MW=60)
print("问题二最优解:", result)
```

```python
# main_problem3.py
from csp_heliostat.optimization.heterogeneous import solve_problem3
result = solve_problem3(warm_start_from_problem2=True)
```

---

## 五、数据流图

```
[配置] ─┐
        ├─→ [layout] ─→ Mirror[N] ─┐
        │                            │
[D, t] ─┴→ [solar_position] ─→ SunState ┐
                                          ├→ [snapshot] ─→ η_i(t)
                                          │
[DNI=H,α_s]──→ [dni.py] ───────────────┤
                                          │
[geometry ŝ, r̂, n̂] ──→ [cosine.py] ────┤
                                          │
[mirror geometry] ──→ [shadow_blocking] ┤
                                          │
[receiver] ──→ [truncation] ─────────────┤
                                          │
[distance] ──→ [atmosphere] ─────────────┤
                                          └→ [total] ─→ P_field
                                                 ↓
                            [annual] ─→ 加权平均 ─→ P̄_field, η̄_field
                                                 ↓
                            [uniform / zoned / heterogeneous]
                                                 ↓
                                            best_params
```

---

## 六、关键算法实现难点与解决方案

### 难点 1：阴影遮挡的 N² 复杂度

**问题**：N=3000 时 O(N²) = 9M 次检查。

**解决**：
1. **空间分桶**：用 Morton 编码把场地切 1×1 m 方格，每镜归属唯一 bucket
2. **KD-tree**：cKDTree.query_ball_tree 找 9 邻域内的镜
3. **Numba JIT**：预编译内层循环

### 难点 2：截断判断的几何求交

**问题**：光线-圆柱求交，浮点误差大。

**解决**：
- 用解析几何直接求解光线参数 t
- 接受精度 ≤ 1 mm（公差合理）

### 难点 3：优化中的混合变量

**问题**：环数是整数，镜位是连续。

**解决**：
- **外层**：枚举 n_rings（5..30），共 26 种
- **内层**：连续变量优化
- 取所有 n_rings 中的最优

### 难点 4：年平均权重选择

**问题**：60 时刻无法做到小时分辨率，权重影响结果。

**推荐**：使用 `weight = max(0, sin α_s)` 加权，物理意义为"日照辐射的相对贡献"。

### 难点 5：异构优化的局部最优

**解决**：
- **多次重启**：随机起始点 × 10 次
- **CMA-ES**：协方差自适应
- **初始种来自均匀解**：warm-start 显著提升收敛

---

## 七、测试与验证

### 7.1 单元测试（tests/）

| 模块 | 测试目标 |
|------|----------|
| solar_position | 春分/夏至/冬至 α_s 与文献值偏差 < 0.1° |
| dni | α_s=90° 时 DNI ≈ 1.366 kW/m²；α_s=10° 时 DNI 应 < 0.5 |
| cosine | 镜面正对太阳 → η_cos=1；平行 → 0 |
| shadow_blocking | 单镜孤立 → η_sb=1；前后两镜 → 后镜 η_sb<1 |
| truncation | 远场镜 η_trunc 较高（光斑大），近场较低 |
| atmosphere | 标准距离（如 1km）→ 0.97 ± 0.01 |

### 7.2 集成验证

| 验证项 | 期望 |
|--------|------|
| 问题一基准场景 P̄ | 应在 30-90 MW 范围（视布局） |
| 额定功率约束 | 问题二/三解必须满足 P̄ ≥ 60 MW |
| 物理合理性 | 单位面积功率率与文献 [NREL/SolarPaces] 偏差 < 30% |

### 7.3 复现性检查

固定 random_seed，记录所有随机源，蒙特卡洛结果波动 < 1%。

---

## 八、开发顺序与里程碑

| 阶段 | 内容 | 预计模块 | 周期 |
|------|------|----------|------|
| **M1** | 物理基础 | solar_position, dni, geometry, atmosphere | 2-3 天 |
| **M2** | 镜场建模 | layout, mirror, constraints | 1-2 天 |
| **M3** | 五项效率 | cosine, sb, trunc | 3-4 天 |
| **M4** | 仿真装配 | snapshot, annual, composition | 1 天 |
| **M5** | 问题一（基准） | 主入口、验证 | 1 天 |
| **M6** | 问题二（均匀优化） | uniform.py + DE | 2-3 天 |
| **M7** | 问题三（异构优化） | heterogeneous.py + 局部搜索 | 3-4 天 |
| **M8** | 可视化与文档 | layout_plot, heatmap, README | 2 天 |
| **总计** | | | **15-19 天** |

---

## 九、性能预算

| 步骤 | 目标耗时 |
|------|----------|
| 单时刻仿真（N=3000） | < 2 s（含阴影遮挡） |
| 全年 60 时刻 | < 2 min（无并行） |
| 问题一最终结果 | < 5 min |
| 问题二 DE 优化（200 代） | < 30 min |
| 问题三 异构搜索 | < 2 h |

**加速手段**：
- Numba JIT 热函数（`shadow_blocking`、`truncation`）
- joblib 并行（年循环 60 个时刻并行）
- 预计算所有镜对距离 / 角度（一次性 O(N²)，避免重复）

---

## 十、交付物清单

| 文件 | 内容 |
|------|------|
| `src/csp_heliostat/` | 完整模块化源码 |
| `tests/` | 单元测试（pytest） |
| `outputs/figure/` | 布局图、热力图、年曲线图 |
| `outputs/data/` | 中间结果 CSV / NPY |
| `报告.md` | 解题思路、关键公式、最终数值结果 |
| `README.md` | 复现指南、依赖说明 |
| `requirements.txt` | Python 依赖 |

---

## 十一、风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 物理模型误差累积（沙堆） | 结果失真 | 用文献基准场景校准（参考 NREL SAM 案例） |
| 优化陷入局部最优 | 解非全局 | 多起点 + 启发式 + 局部精修 |
| 蒙特卡洛波动 | 不稳定 | 固定种子；用更密的栅格 |
| numpy 大数组内存 | 性能问题 | 分批处理或稀疏矩阵 |
| 时间预算超限 | 答不完 | 优先级：M1→M5（拿到问题一）→M6→M7 |

---

## 十二、给执行 Agent 的最后提醒

1. **必须先把 `积日 D 的定义`确认清楚**（0.3 节歧义点），写在前置 README 中
2. **坐标系文档化**：东(x)、北(y)、天(z)；方位从南顺时针为正；`SunState` 注释清楚
3. **效率计算顺序不要拆开**：先做问题一整套链路通，再优化
4. **不要一开始就优化问题三**，先用问题二 warm-start 问题三
5. **所有中间结果都保存**（CSV/NPY），便于临时切换策略
6. **必须用 Numba 或矢量化**，纯 Python 循环会超时
7. **可视化用 matplotlib（静态）+ plotly（交互）双备份**，PPT 用前者
8. **最终输出三个问题的关键数表 + 三张关键图**

---

## 附录 A：核心公式速查

| 符号 | 含义 | 公式 |
|------|------|------|
| δ | 赤纬角 | 23.45°·sin(2π(284+D)/365) |
| ω | 时角 | 15°·(t-12) |
| α_s | 太阳高度角 | sin⁻¹(sin φ sin δ + cos φ cos δ cos ω) |
| γ_s | 太阳方位角 | atan2(sin ω, cos ω sin φ − tan δ cos φ) |
| DNI | 直接法向辐照 | G_0·[a(H)+b(H)·exp(−c(H)/sin α_s)] |
| n̂ | 镜面法向 | (ŝ+r̂)/\|ŝ+r̂\| |
| η_cos | 余弦效率 | max(0, n̂·ŝ) |
| η | 总光学效率 | η_sb·η_cos·η_at·η_trunc·η_ref |
| P | 输出热功率 | Σᵢ Aᵢ·DNI·ηᵢ |

---

## 附录 B：参考实现骨架（完整可执行 demo）

```python
# demo_single_snapshot.py —— 一个最小可运行示例
import numpy as np

LAT = 39.4
H_KM = 3.0
G0 = 1366.0

# 1. 太阳位置：春分正午
D = 0; t = 12
delta = 23.45 * np.sin(np.deg2rad(360*(284+D)/365))
omega = 15*(t-12)
phi = LAT
sin_as = np.sin(np.deg2rad(phi))*np.sin(np.deg2rad(delta)) + \
         np.cos(np.deg2rad(phi))*np.cos(np.deg2rad(delta))*np.cos(np.deg2rad(omega))
alpha_s = np.rad2deg(np.arcsin(sin_as))
print(f"春分正午 α_s = {alpha_s:.2f}°（期望约 50.6°）")

# 2. DNI
a = 0.4237 + 0.00821*H_KM - 0.000505*H_KM**2 + 0.000032*H_KM**3
b = 0.5055 + 0.00595*H_KM - 0.000360*H_KM**2 + 0.000021*H_KM**3
c = 0.2711 + 0.01858*H_KM - 0.001180*H_KM**2 + 0.000060*H_KM**3
DNI = G0 * (a + b*np.exp(-c/sin_as))
print(f"DNI = {DNI:.1f} W/m²")
```

执行此 demo 应得到 α_s ≈ 50.6°，DNI 数值在 1000-1100 W/m² 范围。

---

*文档结束*
