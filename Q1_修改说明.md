# Q1 脚本修改说明 — 给执行 Agent 的对接文档

> **生成时间**：2026-07-25
> **修改文件**：`E:\homework\开发\数学建模\光热发电\开发文件\run_q1_simple.py`
> **修改原因**：原 52 变量方案导致 `popsize × N` 种群爆炸（520 个个体），单次启动 4.8 小时不输出
> **修改方案**：25 环分 5 组共享参数 + 多进程并行

---

## 一、根因诊断（已确认）

### 现象
脚本 `run_q1_two_phase.py` 打印完"阶段 1"标题后卡死，10+ 分钟零输出。

### 根因
scipy `differential_evolution` 的 `popsize` 是**乘数**，实际种群规模 = `popsize × N`（变量数）：

```
原方案参数：
  N（变量数）= 2 + 25环 × 2 = 52
  popsize = 10
  实际种群 = 10 × 52 = 520 个个体  ← 不是 10！

单次评估 ≈ 33 秒（实测）
第一代评估整个种群 = 520 × 33s = 17,160s ≈ 4.8 小时
```

这就是"打印完标题后卡死"的真正原因——DE 正在评估 520 个初始个体，10 分钟才完成约 3.5%。

### 验证方法（5 秒）
```python
from scipy.optimize import differential_evolution
import numpy as np
calls = [0]
def fake_obj(p):
    calls[0] += 1
    return float(np.sum(p**2))
differential_evolution(fake_obj, bounds=[(-1,1)]*52, maxiter=1, popsize=10, seed=42, polish=False)
print(f"实际种群规模 = {calls[0]}")  # 打印 520
```

---

## 二、修改方案

### 2.1 核心改动：25 环分 5 组共享参数

**原方案**（52 变量）：每环独立的 (W, h)
```
params = [x_t, y_t, W_0, h_0, W_1, h_1, ..., W_24, h_24]  # 52 维
```

**新方案**（12 变量）：5 组共享 (W, h)，每组覆盖 5 环
```
params = [x_t, y_t, W_g0, h_g0, W_g1, h_g1, W_g2, h_g2, W_g3, h_g3, W_g4, h_g4]  # 12 维

分组映射：
  环 0-4   → 组 0（内环）
  环 5-9   → 组 1
  环 10-14 → 组 2
  环 15-19 → 组 3
  环 20-24 → 组 4（外环）
```

**分组映射代码**：
```python
for k in range(N_RINGS):  # k ∈ [0, 24]
    g = int(k * N_GROUPS / N_RINGS)  # g ∈ [0, 4]
    W_k = group_W[g]  # 取该组的 W
    h_k = group_h[g]  # 取该组的 h
```

### 2.2 性能对比

| 方案 | N | popsize | 实际种群 | 第一代耗时 | 25代总耗时 |
|------|---|---------|---------|-----------|-----------|
| 原（52变量） | 52 | 10 | 520 | 4.8 h | ~120 h ❌ |
| 新（5组12变量） | 12 | 5 | 60 | 33 min | ~14 h（串行）|
| 新 + 8核并行 | 12 | 5 | 60 | 4 min | **~1.7 h** ✅ |

### 2.3 对拟合效果的影响

**影响很小**，理由：
1. 已有数据显示异构收益本就有限（52 变量 DE 最终往均一解走）
2. 5 组能捕捉**径向梯度**——定日镜场异构优化的主要物理方向
3. 角度方向异构收益小（太阳轨迹南北对称）
4. 真实塔式电站（Ivanpah、Gemasolar）也采用分组均一方案

详见之前的《Q1 分组方案对拟合效果影响分析》。

---

## 三、脚本关键改动清单

### 3.1 全局配置

```python
N_GROUPS = 5                        # 新增
N_RINGS = 25
N_VARS = 2 + 2 * N_GROUPS           # = 12（原 52）

PHASE1_POPSIZE = 5                  # 原 10（最小值，避免种群过大）
PHASE2_POPSIZE = 5
```

### 3.2 `_simulate` 函数

**原**：从 `params[2 + 2*k]` 直接取每环参数
**新**：先提取 5 组参数，再按 `g = int(k * N_GROUPS / N_RINGS)` 映射到每环

```python
def _simulate(params, schedule):
    # 提取 5 组参数
    group_W = []
    group_h = []
    for g in range(N_GROUPS):
        group_W.append(float(np.clip(params[2 + 2*g], *MIRROR_W_RANGE)))
        group_h.append(float(np.clip(params[2 + 2*g + 1], *INSTALL_HEIGHT_RANGE)))

    for k in range(N_RINGS):
        g = int(k * N_GROUPS / N_RINGS)  # 分组映射
        W_k = group_W[g]
        h_k = group_h[g]
        # ... 后续布局逻辑不变
```

### 3.3 `build_init_pop` 函数

**原**：生成 `(popsize, 52)` 数组，每环独立扰动
**新**：生成 `(popsize, 12)` 数组，每组扰动

```python
def build_init_pop(popsize):
    base = np.zeros(N_VARS)  # 12 维
    base[0], base[1] = x_t, y_t
    for g in range(N_GROUPS):
        base[2 + 2*g] = W0      # 5 组都用问题二的 W
        base[2 + 2*g + 1] = h0

    pop = [base.copy()]
    for _ in range(popsize - 1):
        p = base.copy()
        p[0] += np.random.uniform(-10, 10)
        p[1] += np.random.uniform(-10, 10)
        for g in range(N_GROUPS):
            p[2 + 2*g] *= np.random.uniform(0.9, 1.1)
            p[2 + 2*g + 1] *= np.random.uniform(0.9, 1.1)
        # ...
```

### 3.4 DE 调用：新增并行参数

```python
result_p1 = differential_evolution(
    obj_phase1, bounds=BOUNDS,
    maxiter=PHASE1_MAXITER, popsize=PHASE1_POPSIZE,
    tol=1e-8, seed=42,
    init=init_pop, polish=False,
    callback=cb_phase1,
    workers=-1,            # ← 新增：用所有 CPU 核心
    updating='deferred',   # ← 新增：并行模式必须
)
```

**注意**：
- `workers=-1` 使用 `multiprocessing`，子进程会重新 import 模块 + Numba 重新编译（首次 30-60 秒开销，后续无）
- `updating='deferred'` 是并行模式的必要配置

### 3.5 输出格式

**新增 `per_group` 字段**（替代原 `per_ring`）：

```json
{
  "per_group": [
    {"group": 0, "W": 5.8, "h": 3.0},
    {"group": 1, "W": 6.0, "h": 3.0},
    {"group": 2, "W": 6.1, "h": 3.1},
    {"group": 3, "W": 6.2, "h": 3.2},
    {"group": 4, "W": 6.3, "h": 3.3}
  ]
}
```

---

## 四、验收标准

运行 `run_q1_simple.py` 后应满足：

### 4.1 性能
- [ ] "Testing first eval..." 在 60 秒内打印完成
- [ ] "Phase 1 done" 在 2 小时内打印
- [ ] "Phase 2 done" 在 1 小时内打印
- [ ] 总耗时 < 3 小时

### 4.2 输出文件
- [ ] `outputs/data/problem3_convergence_v2.csv` 存在
  - 包含 33 行（25+8 代）
  - 包含 `constraint_estimated` 和 `constraint_actual` 两列
- [ ] `outputs/data/problem3_result_v2.json` 存在
  - `n_vars = 12`
  - `n_groups = 5`
  - `per_group` 包含 5 组参数

### 4.3 数值合理性
- [ ] `best_P_avg_MW` 在 55-70 MW 范围
- [ ] `best_P_per_area_W_m2` 在 500-600 W/m² 范围
- [ ] 5 组参数没有极端值（W ∈ [5, 7], h ∈ [2.5, 3.5]）

---

## 五、运行方式

```bash
cd E:\homework\开发\数学建模\光热发电\开发文件
python run_q1_simple.py
```

**预期首次输出**（30 秒内）：
```
============================================================
Q1: 两阶段 DE（5组分组方案）
  变量数 N = 12（原 52）
  分组数 = 5（每 5 环共享参数）
  校准系数 k = 0.922955
  种群规模 = popsize×N = 5×12 = 60
============================================================

=== Phase 1: noon DE (25 gen, pop 5) ===
Init pop shape: (5, 12) (应为 (5, 12))
Testing first eval...
First eval: -0.000567 (33.2s)
Starting DE Phase 1 (并行 workers=-1)...
  [Ph1] gen  0: P_noon=65.19, P_est=60.18, P_full=N/A, constraint=None
  ...
```

**如果 30 秒后仍卡在 "Testing first eval..."**：
- 说明 `_simulate` 仍在执行（正常，单次约 33 秒）
- 再等 30 秒应打印 "First eval" 结果

**如果 2 分钟后仍无输出**：
- 检查 Numba 编译是否卡住（首次编译可能 1-2 分钟）
- 检查 `workers=-1` 是否导致死锁（尝试改为 `workers=1` 排查）

---

## 六、回退方案

如果 5 组方案运行仍有问题，可进一步降级：

### 6.1 关闭并行
```python
# 把 workers=-1 改为 workers=1
workers=1,
updating='immediate',  # 改回立即更新
```
性能降为串行（约 14 小时），但避免多进程潜在问题。

### 6.2 进一步减少分组
```python
N_GROUPS = 3  # 改为 3 组（内/中/外）
N_VARS = 2 + 2*3  # = 8
```
种群降到 5×8=40，速度更快但精度略降。

### 6.3 极端情况：直接用均一解
如果时间极度紧张，可以跳过 DE，直接用问题二的均一解作为问题三答案：
```python
# 直接构造均一解，不做优化
final_params = np.zeros(12)
final_params[0], final_params[1] = 0, 0
for g in range(5):
    final_params[2 + 2*g] = 6.0
    final_params[2 + 2*g + 1] = 3.0
final = _simulate(final_params, FULL_SCHEDULE)
# 结论："5 组分组与均一无显著差异，采用均一配置"
```

---

## 七、与原任务书的差异说明

本脚本对 `审查问题与改进任务书.md` 的 Q1 部分做了以下调整：

| 项 | 原任务书 | 本脚本 | 原因 |
|----|---------|--------|------|
| 变量数 | 52（每环独立） | 12（5 组共享） | 性能：种群从 520 降到 60 |
| popsize | 15（阶段1）/ 5（阶段2） | 5（两阶段统一） | 配合分组降维 |
| 并行 | 未指定 | `workers=-1, updating='deferred'` | 进一步加速 |
| 输出字段 | `per_ring`（25 项） | `per_group`（5 项） | 对应分组结构 |
| 结论表述 | 3 种情况 | 保持 3 种情况 | 逻辑不变 |

**核心结论**：分组方案不改变问题三的物理结论，只改变搜索空间维度。如果分组方案仍收敛到均一解，则"均一最优"的结论**更加可信**（因为连径向梯度异构都突破不了）。

---

*文档结束。执行 `run_q1_simple.py` 后，请对照第四节验收标准逐项检查。*
